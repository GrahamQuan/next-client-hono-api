/**
 * current runtime is node.js
 */

import { serve } from '@hono/node-server';
import App from './app';
import env from '~/lib/env';

serve(
  {
    fetch: App.fetch,
    port: env.PORT,
  },
  (info) => {
    console.log(`Server running on port: ${info.port}`);
  }
);
