/**
 * 1. current runtime is cloudflare worker
 * 2. wanna change runtime? see entry templates in `<root>/src/__index_templates__` folder
 * 3. make sure your runtime supports `process.env`
 */

import App from './app';

export default App;
